{
  "name": "ielts-roadmap",
  "private": true,
  "version": "1.0.0",
  "description": "Build pipeline for the IELTS 6-Month Roadmap static site (Tailwind CLI compile step; the site itself is plain HTML/CSS/JS with no bundler/framework).",
  "scripts": {
    "build:css": "tailwindcss -i ./tailwind.input.css -o ./tailwind.compiled.css -c ./tailwind.config.js --minify",
    "watch:css": "tailwindcss -i ./tailwind.input.css -o ./tailwind.compiled.css -c ./tailwind.config.js --watch",
    "build": "python3 build.py site"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
